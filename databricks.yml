# This is a Databricks asset bundle definition for dab_test.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_test
  uuid: 77deab9f-ec01-4612-b7f4-ccd006a7f488

include:
  - resources/dab_test.job.yml
  # - resources/*/*.yml

variables:
  env_name:
    description: name of env
    default: default-env

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-984752964297111.11.azuredatabricks.net/

  prod:
    mode: production
    workspace:
      host: https://adb-984752964297111.11.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/sourav.gulati@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/sourav.gulati@databricks.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: sourav.gulati@databricks.com
        level: CAN_MANAGE
